import './Toolbar.css';

const Toolbar = ({ onConvertToMarkdown, onConvertToHtml, activeTab }) => {
  return (
    <div className="toolbar">
      {/* HTML編集を無効化したため、変換ボタンも非表示にします */}
    </div>
  );
};

export default Toolbar;